import '@testing-library/jest-dom'

// Возможно, лишняя библиотека
// Устанавливали, чтобы исправить ошибку, когда в компоненте используется асинхронная функция, но у меня такой ошибки не было
import 'regenerator-runtime/runtime'
